# 屏幕刷新性能分析报告

测试日期：2026-05-29  
测试方式：固件内置 `DWT->CYCCNT` 计时，通过 `USART1` 输出性能日志。  
串口参数：`115200 8N1`  
CPU 主频：`HCLK=72MHz`

> 本报告中的 FPS 是“MCU 完成一次 UI 绘制/刷新函数的等效 FPS”，不是 TFT 面板自身刷新率。

## 原始日志

```text
[PERF] UART1 115200 HCLK=72MHz
[PERF] sensor read 11492us
[PERF] boot benchmark start
[PERF] LCD full fill   1321561us fps=0.7 tx=227KB/s
[PERF] Draw home       7821477us fps=0.1
[PERF] Home dynamic    168820us fps=5.9
[PERF] Draw wave       5690409us fps=0.1
[PERF] Wave dynamic    2416393us fps=0.4
[PERF] Peixiao bmp     34763us fps=28.7 tx=220KB/s
[PERF] boot benchmark end
[PERF] sensor read 11492us
[PERF] refresh home  part 168819us fps=5.9
[PERF] sensor read 11492us
[PERF] refresh home  part 168820us fps=5.9
[PERF] sensor read 11492us
[PERF] refresh home  part 168820us fps=5.9
[PERF] sensor read 11492us
[PERF] refresh home  part 168820us fps=5.9
```

## 指标换算

| 指标 | 耗时 | 等效 FPS | 结论 |
|---|---:|---:|---|
| 传感器读取 | 11.492 ms | - | 很快，不是 UI 卡顿瓶颈 |
| 全屏纯色填充 | 1321.561 ms | 0.7 FPS | LCD 像素写入吞吐很低 |
| 首页整页绘制 | 7821.477 ms | 0.1 FPS | 页面切换会明显等待 |
| 首页局部刷新 | 168.820 ms | 5.9 FPS | 可用于 2 秒一次数据刷新，但不算流畅 |
| 波形页整页绘制 | 5690.409 ms | 0.1 FPS | 页面首次进入耗时明显 |
| 波形页动态刷新 | 2416.393 ms | 0.4 FPS | 波形实时刷新严重偏慢 |
| 人物图片绘制 | 34.763 ms | 28.7 FPS | 人物图片不是主要瓶颈 |

## 关键判断

1. **最大瓶颈是 LCD 像素写入吞吐**

   全屏分辨率是 `480 * 320`，RGB565 每像素 2 字节，单次全屏数据量约：

   ```text
   480 * 320 * 2 = 307200 bytes = 300 KiB
   ```

   全屏填充耗时 `1.321561s`，实测吞吐约 `227 KiB/s`。人物图片绘制吞吐约 `220 KiB/s`，两者非常接近，说明瓶颈主要在 LCD 像素写入链路，而不是某一个 UI 函数。

2. **首页整页绘制过慢**

   首页整页绘制 `7.82s`，约等于 `5.9` 次全屏填充耗时。说明整页 UI 里存在大量区域填充、圆角/阴影、文字绘制、图标绘制等操作，叠加后成本很高。

3. **首页局部刷新目前可接受**

   首页局部刷新稳定在 `168.82ms`，对应 `5.9 FPS`。因为传感器是每 2 秒刷新一次，所以当前首页数据更新不会阻塞很久，但如果想做肉眼无感的高频动画，这个速度仍然偏慢。

4. **波形页是最需要优化的页面**

   波形页动态刷新 `2.416s`，只有 `0.4 FPS`。这说明当前波形刷新仍然画了太多内容，尤其是重复绘制波形框、文字、曲线历史点等，已经不适合实时刷新。

5. **传感器读取不是问题**

   传感器读取约 `11.5ms`，相比 UI 绘制耗时很小。优化重点不应放在 AHT20/BMP280 读取上。

## 优化优先级

### P0：波形页只更新变化部分

当前 `Wave dynamic` 耗时过长，应优先改成：

- 波形背景、标题、坐标框只在进入页面时绘制一次。
- 数据更新时只擦除旧线段并绘制新线段。
- 不要每次重画三张完整波形图。
- 必要时降低波形点数，例如从 `80` 点降到 `48` 或 `60` 点。

目标：把波形动态刷新从 `2416ms` 降到 `200ms` 以内。

### P1：减少全页重绘

页面切换时整页绘制非常慢，应尽量避免频繁 `LCD_Clear()` 和全屏级 UI 重画：

- 静态 UI 和动态数值分离。
- 页面切换只绘制一次静态框架。
- 数值变化只刷新数字所在小矩形区域。

目标：首页/波形页进入时间先降到 `2s` 以内。

### P2：简化高成本 UI 装饰

当前 UI 中圆角、阴影、图标线条和多区域填充都会增加像素写入量。

建议：

- 大面积圆角卡片改成普通矩形或小半径矩形。
- 阴影只保留一层浅色偏移，或者直接去掉。
- 少用填充圆和大量细线图标。
- 首页动态区域尽量只更新文字，不重复画图标。

### P3：优化 LCD 底层吞吐

当前 LCD 写入吞吐约 `220~227 KiB/s`。如果希望达到更流畅的刷新，底层吞吐需要明显提升。

参考需求：

| 目标 | 全屏数据量 | 所需吞吐 |
|---|---:|---:|
| 1 FPS | 300 KiB/s | 当前略低于此目标 |
| 5 FPS | 1500 KiB/s | 约为当前 6.6 倍 |
| 10 FPS | 3000 KiB/s | 约为当前 13.2 倍 |

可考虑的方向：

- 检查 LCD 写数据函数是否存在过多 GPIO 翻转开销。
- 如果硬件连接允许，提升 SPI/LCD 总线速度。
- 如果后续资源允许，考虑 DMA 或批量连续写入窗口。

## 当前结论

当前系统的卡顿主要来自 **LCD 像素写入速度低 + UI 绘制面积过大**。  
传感器读取和人物图片不是主要瓶颈。

最值得先做的优化是波形页：将三条波形从“整块重绘”改成“局部增量绘制”。首页目前的局部刷新 `168ms` 可以接受，但整页绘制 `7.8s` 需要继续压缩。

